/**
 * Закрытие панели при уходе на любой НЕ перехваченный маршрут (серии, группа
 * шотов, вход). Без этого слот вёл бы себя неожиданно: параллельный роут при
 * клиентской навигации на неподходящий адрес остаётся видимым, и панель висела
 * бы поверх экрана серии. Перехваченные адреса ((.)bible и прочие) матчатся
 * точнее этого catch-all, поэтому он их не перекрывает.
 */
export default function CatchAll() {
  return null;
}
