/**
 * Слот @panel пуст, пока не активен перехваченный роут вкладки (Библия,
 * Очередь, Консоль, Настройки, Затраты). Нужен для ЖЁСТКОЙ навигации: по
 * прямому URL и перезагрузке слот не может восстановить состояние, падает сюда,
 * и маршрут открывается своей полной страницей (как задумано — deep-link живой).
 */
export default function Default() {
  return null;
}
